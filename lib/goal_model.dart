class GoalModel {
  String name;
  String description;
  DateTime targetDate;
  int targetAmount;
  int currentAmount;

  GoalModel(this.name, this.description, this.targetDate, this.targetAmount,
      this.currentAmount) {}
}
